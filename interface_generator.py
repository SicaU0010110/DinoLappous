import ast
import os
from pathlib import Path

class InterfaceGenerator:
    def __init__(self, project_root="."):
        self.root = Path(project_root)

    def generate_all(self):
        # Mock logic based on user prompt intent
        return []
